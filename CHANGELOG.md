# 更新日志

本项目采用语义化版本号。尚未发布的变更记录在 `Unreleased`。

## [Unreleased]

### 新增

- Windows 安装包（Inno Setup）：`installer/DeltaCrafter.iss` 与 `scripts/build-installer.ps1`，
  产出 `DeltaCrafter-Setup-<版本>.exe`，自带卸载程序（停止进程、清除开机自启计划任务，
  并询问是否删除本机数据，默认保留）。
- 固定并验证官方 Inno Setup 6.7.3 与仓库内简体中文语言文件；缺失、篡改或 payload
  版本不一致时构建直接失败。
- 卸载失败不再被吞掉；静默卸载不显示自定义确认框并保留数据，交互卸载仅在卸载完成后
  执行用户确认的数据删除。
- Release 工作流在 `v*` 标签下同时构建并上传 Setup、免安装 ZIP 及其 SHA-256。

### 修复

- 固定编译器安装改为显式等待 GUI 安装进程并读取真实退出码，避免 PowerShell 7
  将空的 `$LASTEXITCODE` 误判为安装失败。

## [0.1.0] - 2026-07-27

### 新增

- Windows 11 Fluent/WinUI 3 桌面界面与托盘控制。
- 四设施制造计划、当前任务识别、领取、续造和取消制造。
- Windows 简体中文 OCR、多倍率多遍共识与物品目录规范化。
- 游戏内倒计时驱动的自动调度、失败退避、预启动和近时间衔接。
- 归一化锚点、诊断截图、运行日志、自启、防睡眠与三种收尾行为。

### 验证

- Release/x64 构建通过，0 警告、0 错误。
- 核心单元测试 39 项通过。
- 2560×1440 下端到端领取与续造流程实机通过。

### 已知限制

- 预启动、近时间衔接、KeepAtLobby、整夜自动循环与多 DPI 仍需扩大实机验证。
