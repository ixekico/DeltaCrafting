# 更新日志

本项目采用语义化版本号。尚未发布的变更记录在 `Unreleased`。

## [Unreleased]

### Release notes

#### 修复与优化

- 更新公告只显示新增功能和修复优化，完整的文档、验证结果与已知问题仍保留在
  `CHANGELOG.md`，不再挤占用户的更新窗口。

### 变更

- 发布工作流改为只导出同版本的「发布说明」小节；缺少摘要时明确失败，避免再次把
  面向维护者的完整版本记录直接发布给用户。

## [0.4.0] - 2026-07-30

### Release notes

#### 新增功能

- 四个制造设施现在可以分别选择「自定义物品」「每小时利润最高」或「总利润最高」。
- 检测到新版本时，更新窗口会直接列出新版本的更新内容。

#### 修复与优化

- 行情会在启动时预先获取并于每个整点更新；缓存为空时切换利润模式会立即获取，
  不再因临时联网而长时间没有变化。
- 从利润模式切回自定义模式时，会恢复该设施上一次手动选择的物品。
- 旧版本的全局制造模式会自动迁移到四个设施，原有物品和启用状态不会丢失。

### 新增

- 应用检测到新版本时，更新窗口直接显示 GitHub Release 中的该版本更新日志。
- 制造中心的四个设施可分别选择「自定义物品」「每小时利润最高」或「总利润最高」；
  利润推荐只接管选择了相应模式的设施，允许四张设施卡使用不同策略。

### 变更

- GitHub Release 正文改为由发布工作流从 `CHANGELOG.md` 的对应版本章节提取，
  不再只生成「Full Changelog」比较链接；缺少对应章节或内容为空时发布失败。
- 主窗口系统标题和自定义标题栏统一显示当前版本号，例如「三角洲特勤助手v0.4.0」。
- 制造模式从设置页移到每张设施卡，并放在「启用」开关左侧；删除设施备注字段。
- 首次升级到 v0.4.0 时，旧版全局制造模式会一次性迁移到四个设施，现有制造物品与
  启用状态保持不变，迁移完成后不再保留全局模式配置。
- kkrb.net 行情改为应用启动后立即预热、随后每个本地整点后台刷新，与设施是否启用
  利润模式无关；切换制造模式优先读取最近成功缓存，不再重复联网等待。刷新失败保留
  旧缓存，并在 10 分钟后或更早到来的下一整点明确重试。
- 若切换到利润模式时行情缓存仍为空，会立即唤醒一次单通道获取；启动预热正在进行时
  复用同一次请求，避免重复并发抓取，成功后自动应用当前所有利润模式设施。
- 每个设施独立保存最后一次自定义物品及其 OCR 匹配名；利润推荐只改当前制造物品，
  从利润模式切回自定义时恢复原选择，利润模式之间切换不会覆盖自定义记忆。
- 制造计划 schema 升至 3；旧计划首次升级时以当下物品初始化自定义记忆。旧版本已经
  覆盖且从未保存的更早自定义选择无法可靠反推，需要用户重新选择一次。

### 文档

- README 按标准开源项目结构重组，补充项目图标、状态徽章、快速入口、目录、制造模式
  对照表、项目结构及更清晰的安装、使用、安全与贡献说明。
- 免责声明只保留非法用途警示，不再限制商业用途，与 MIT 许可证授予的权利保持一致。

### 验证

- Release/x64 构建通过，0 警告、0 错误；核心单元测试 95 项通过，覆盖旧版全局模式
  迁移、混合设施模式保留、新版本配置拒绝降级、执行计划快照隔离，以及成功/失败时
  的整点刷新调度和自定义物品在模式往返后的恢复。
- Inno Setup 6.7.3 成功生成 v0.4.0 Setup、免安装 ZIP 与两份 SHA-256 校验文件；
  payload 和安装程序产品版本均为 0.4.0，两份校验值复算一致。
- 本地 v0.4.0 构建界面检查通过：设置页已移除全局模式；设施卡在普通窗口下单列、
  最大化后 2×2 排列正常；三项模式位于「启用」左侧且文字完整，备注栏已移除。

### 已知问题

- 四个设施混合制造模式已通过模型、迁移、编译与单元测试，尚未使用实时行情完成
  混合组合的整轮制造实机验证；125%/150% 缩放仍需在 v0.4.0 安装包验收时复查。

## [0.3.1] - 2026-07-30

### 变更

- 项目许可证由“保留全部权利”切换为 MIT，README 许可证说明同步更新。

### 新增

- 补齐下级材料或领取制造完成物品时，若游戏提示「仓库空间不足」，立即中止本轮并
  弹出系统通知，提醒用户及时清理仓库；不再重复点击购买或领取。

### 修复

- 1920×1080 模式选择页改用两种卡片样式都稳定存在的「战役模式」判定；大厅
  「行前备战」探针使用实测稳定的 1.5 倍 OCR，修复默认 2 倍下「战」误读为「找」。
- Setup 完成页的「立即运行」显式使用安装程序当前的管理员令牌，修复应用要求提权时
  Windows 返回错误 740。

### 验证

- 14 张 1920×1080 关键页面/状态截图（含一张实际客户区 1919×1080）全部通过生产
  OCR 引擎回放；两张 2560×1440 仓库已满截图均准确识别「仓库空间不足」。
- Release/x64 构建通过，0 警告、0 错误；核心单元测试 81 项通过。
- Inno Setup 6.7.3 成功生成 v0.3.1 Setup、免安装 ZIP 与两份 SHA-256 校验文件。
- v0.3.1 Setup 静默覆盖安装成功，安装日志确认以安装程序当前管理员令牌启动；
  `C:\Program Files\DeltaCrafter\DeltaCrafter.exe` 自动启动成功，未出现错误 740。

### 已知问题

- 1920×1080 当前完成的是关键页面/状态回放，尚未完成该分辨率下的整轮自动制造实机验证；
  其他 16:9 分辨率也仍需扩大样本。

## [0.3.0] - 2026-07-28

### 新增

- 设置「游戏」栏新增「制造模式」:自定义(默认,保持手选物品)/每小时利润优先/
  总利润优先。利润优先模式按 kkrb.net「特勤处制作产物推荐」自动填充四个设施的
  制造物品,每 2 小时刷新一次;此模式下计划页物品锁定为只读,启用开关与备注仍可
  修改,切回自定义即恢复手选。推荐获取失败在计划页横幅亮明原因并自动重试,
  不清空、不回退现有计划;每个设施分别读取总利润最高与每小时利润最高物品,
  两种口径允许给出不同推荐。
- 左侧导航的「Settings」改为中文「设置」(禁用系统内置项,改用自定义底部项,
  文案不再跟随系统语言)。

### 修复

- 托盘菜单退出时先同步隐藏通知区图标、释放应用资源，再显式终止 WinUI
  应用，避免右键退出后图标和进程继续残留数秒。
- 每小时利润模式改为读取 `sphData`,总利润模式读取 `spData`;不再从总利润推荐物品的
  制造档位反推每小时推荐,技术中心等设施在两种口径下可正确选择不同物品。
- 利润推荐等待当前制造轮结束后再整批写入;每轮制造使用隔离的计划快照,避免刷新时
  四个设施混用新旧物品,或出现物品显示名与 OCR 匹配名不一致。
- 剩余时间解析支持实测 OCR 变体 `剩 余 时 间 ： 07 、 59 52`;仅在标签完整且
  恰好存在三个合法时间数字组时采信,仍拒绝越界、残缺或多余数字。

### 文档

- README 增加开源免费、仅供学习交流、禁止非法及商业用途的免责声明。

### 验证

- Release/x64 构建通过,0 警告、0 错误;核心单元测试 81 项通过(覆盖两套推荐
  分流、设施映射、执行计划快照、超时翻译、握手序列与残缺数据拒绝路径)。
- 推荐抓取实网冒烟通过,推荐物品随行情实时变化;站点瞬时超时按显式超时处理并
  自动重试,不影响巡检循环存活。

### 已知问题

- Setup 安装完成页勾选「立即运行」时可能因权限令牌不匹配显示错误 740；安装本身
  已完成，可从开始菜单或安装目录重新启动。该问题留待后续版本修复。

## [0.2.0] - 2026-07-27

### 新增

- 启动链把活动公告页识别为已知界面(判定词取公告框架底部的「继续」提示),识别到
  立即按 ESC 跳过(上限 8 页,超限保存现场明确失败),不再依赖未知画面的兜底 ESC
  节拍慢跳;anchors 升至 rev8,启动时自动迁移本地副本。
- 应用内检查更新:设置「关于」栏最右新增「检查更新」,每次启动自动检查一次。发现新版
  弹窗确认后从 GitHub Release 下载安装包、校验 SHA-256、静默覆盖安装并自动重启;
  制造计划与正在计时的倒计时数据保存在本机数据目录,升级不受影响。下载/安装期间暂停
  自动调度,不会拦腰截断正在进行的制造。校验不符即删除安装包并报错,绝不带病安装。

### 修复

- 全屏/宽窗口下四个页面的内容列改为确定性居中,不再依赖框架对齐退化(实测 125%
  缩放最大化时会偏右)。
- 制造计划页设施卡由竖排 1×4 改为 2×2 网格,顺序与游戏特勤处一致(技术中心、
  工作台、制药台、防具台);窄窗口自动回落为单列。
- 更新流程以原子执行闸门等待正在运行的制造自然结束,并在取得执行锁后再次检查闸门,
  消除“任务已通过检查但尚未拿锁”时被安装程序中断的竞态。
- Release 资产必须与版本标签精确对应且使用 HTTPS,避免同一 Release 中旧安装包与新
  校验文件被错误配对。

### 验证

- Release/x64 构建通过,0 警告、0 错误;核心单元测试 61 项通过。
- 最大化与窄窗口下验证计划页 2×2/单列自适应顺序;总览、计划、日志和设置页居中正常。
- 深色与浅色主题、设置页手动检查更新及状态反馈实机通过。
- v0.2.0 Setup/ZIP 与两份 SHA-256 生成成功;静默安装/卸载退出码为 0,安装目录和
  自启任务清理完成,本机数据哈希不变。
- `/AutoLaunch=1` 安装后成功启动 Program Files 中的 v0.2.0;清理卸载后计划、
  设置和倒计时状态文件仍保留。

## [0.1.0] - 2026-07-27

### 新增

- Windows 11 Fluent/WinUI 3 桌面界面与托盘控制。
- 四设施制造计划、当前任务识别、领取、续造和取消制造。
- Windows 简体中文 OCR、多倍率多遍共识与物品目录规范化。
- 游戏内倒计时驱动的自动调度、失败退避、预启动和近时间衔接。
- 归一化锚点、诊断截图、运行日志、自启、防睡眠与三种收尾行为。
- Windows 安装包与自动卸载程序；卸载会显式停止进程、清理自启任务，并在用户确认后
  删除本机数据，静默卸载默认保留。
- 固定验证 Inno Setup 6.7.3、简体中文语言文件与 payload 版本；Release 工作流同时
  发布 Setup、免安装 ZIP 及其 SHA-256。

### 修复

- 固定编译器安装显式等待 GUI 进程并读取真实退出码，避免 PowerShell 7
  将空的 `$LASTEXITCODE` 误判为安装失败。

### 验证

- Release/x64 构建通过，0 警告、0 错误。
- 核心单元测试 39 项通过。
- 2560×1440 下端到端领取与续造流程实机通过。
- GitHub `main` CI 与 `v0.1.0` Release 工作流通过；Setup、ZIP 和两份校验文件已公开。

### 已知限制

- 预启动、近时间衔接、KeepAtLobby、整夜自动循环与多 DPI 仍需扩大实机验证。
