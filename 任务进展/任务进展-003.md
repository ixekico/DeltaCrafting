# 任务进展-003

## 2026-07-27 GitHub 首次发布收尾

- ✅ 最新源码 Release/x64 干净构建通过：0 警告、0 错误。
- ✅ 核心单元测试由 32 项扩充到 39 项并全部通过；新增 `TextMatchTests.cs`，
  覆盖「7毛2」、弱标点、同族弹药距离与列表子串匹配。
- ✅ `CountdownParserTests.cs` 增加实测 OCR 分隔符回归：全角冒号、`:0 → ℃`、逗号误读冒号。
- ✅ 新增 `README.md`、`CHANGELOG.md`、`CONTRIBUTING.md`、`SECURITY.md`、
  `THIRD-PARTY-NOTICES.md` 与保守版权声明。
- ✅ 新增 `global.json`、`.editorconfig`、`.gitattributes`，固定 SDK 选择与文本格式。
- ✅ 新增 GitHub Actions：每次推送/PR 自动构建测试；`v*` 标签自动生成
  Windows x64 自包含压缩包、SHA-256 并创建 GitHub Release。
- ✅ 新增 `scripts/build-release.ps1`，本机与 CI 共用同一发布路径，任一步失败即中止。
- ✅ 发布包附带固定依赖版本对应的官方许可证正文与上游声明，位于 `licenses/`。
- ✅ 新增 Bug/功能 Issue 表单与 PR 模板，日志和截图入口明确要求脱敏。
- ✅ 旧动漫图标按用户明确要求继续用于发布；新生成候选已删除且引用全部撤销。
- ✅ `HANDOFF.md` 保留在本机供跨会话协作，但因含本机诊断路径而排除出公开仓库。
- ✅ 完整发布脚本实跑通过：生成 0.1.0 自包含 ZIP 与 SHA-256；压缩包内容审计无内部交接、
  日志、截图、用户配置、构建缓存或候选新图标，校验值复算一致。
- ✅ 本地 Git 仓库已初始化为 `main`，124 个发布文件已通过秘密扫描并进入暂存候选。
- ⏳ 待：确认提交身份、许可证最终选择与 GitHub 远程仓库后提交、推送并创建首个 Release。

## 2026-07-27 发布安装包(Setup + 卸载程序)

- ✅ `installer/DeltaCrafter.iss` 固定 AppId，管理员安装到 Program Files；安装/卸载前显式
  结束托盘进程，卸载清除 `DeltaCrafter-AutoStart`，任一步异常均暴露退出码并停止。
- ✅ 静默卸载不显示自定义提示且保留 `%LOCALAPPDATA%\DeltaCrafter`；交互卸载仅在
  `usPostUninstall` 阶段删除用户确认的数据，避免程序未卸完而数据先丢失。
- ✅ `install-inno-setup.ps1` 固定下载官方签名 Inno Setup 6.7.3，并同时验证发布文件、
  ISCC 与签名发布者；`ChineseSimplified.isl` 固定入库并验证 SHA-256，不设英文回退。
- ✅ `build-installer.ps1` 校验复用 payload 的真实 ProductVersion，拒绝用旧二进制伪装
  新版本；产出 Setup 与独立 SHA-256。
- ✅ `release.yml` 固定 `windows-2025`，真实执行编译器安装与 Setup 构建，Release 同时
  上传 `.exe`、`.zip` 及两份校验值。
- ✅ 完整发布构建通过：0 警告、0 错误、39 项测试通过，Setup 6.7.3 编译成功。
- ✅ 临时目录静默安装/卸载冒烟通过：退出码均为 0，卸载程序、应用列表注册项和安装目录
  按预期生成/移除；测试自启任务被清理，现有本机数据逐文件哈希保持不变。
- ℹ️ Setup 与卸载程序当前未进行商业代码签名，首次下载仍可能触发 SmartScreen。
- ✅ 首次 `main` 推送的 GitHub CI 通过；首轮标签构建定位到 PowerShell 7 调用 GUI 程序时
  不可靠等待，导致空 `$LASTEXITCODE` 被误判为失败。
- ✅ `install-inno-setup.ps1` 改为 `Start-Process -Wait -PassThru` 读取真实退出码；在全新
  临时目录重新安装并复核 ISCC 固定 SHA-256 后通过，测试目录已清理。
