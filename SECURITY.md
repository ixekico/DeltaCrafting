# 安全政策

## 支持版本

安全修复只面向最新 GitHub Release。请先确认问题能在最新版本复现。

## 报告漏洞

请使用 GitHub 仓库的 Private vulnerability reporting 私下提交安全问题，不要创建公开 Issue。

报告应包含：

- 受影响版本与 Windows 版本
- 最小复现步骤
- 实际影响
- 必要的日志片段

请删除游戏昵称、聊天内容、Windows 用户名、绝对路径和截图中的其他个人信息。不要上传账号凭据、会话令牌或完整的 `%LocalAppData%\DeltaCrafter` 目录。

## 范围

以下问题属于安全范围：

- 未经确认执行了错误的输入动作
- 读取、覆盖或泄露预期目录之外的数据
- 可执行文件或更新包完整性问题
- 权限提升、自启计划任务或窗口匹配造成的越权行为

单纯的 OCR 识别失败、游戏 UI 更新或第三方游戏规则变更通常按普通 Bug 处理，除非它们能导致错误输入或数据泄露。
